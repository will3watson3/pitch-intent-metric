# Generated reports

Analysis and inference write generated artifacts here. `scripts/run_intent_pilot.py` produces `intent_pilot/variant_predictions.csv`, `benchmark.csv`, `summary.json`, and its own results README. They require local reviewed data.

Only this directory guide is published. The root README records an aggregate pilot snapshot; generated pitch-level reports remain local.
