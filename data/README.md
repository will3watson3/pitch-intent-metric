# Local data

Place your own pitch-level tables, frame manifests, images, and audit records here. Pipeline runs write to `runs/<run-name>/`. Git ignores everything in this directory except this README.

See [data contracts](../docs/DATA.md) for keys, coordinates, and required pilot filenames. For a fresh checkout, use the synthetic example in `examples/`; no real datasets are bundled.
