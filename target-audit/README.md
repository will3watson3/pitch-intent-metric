# Glove target audit

Start `python scripts/labeler_server.py --port 8765` and open `http://127.0.0.1:8765/target-audit/`.

Review the selected setup frame, step through frames, and confirm or correct the glove target. The current research queue covers Dylan Cease sliders and Tarik Skubal changeups. Reviews persist in `data/target_audit_labels.csv`; the intent merge accepts confirmed/corrected targets. HTML/CSS/JavaScript is served without a build step. See [data contracts](../docs/DATA.md).
